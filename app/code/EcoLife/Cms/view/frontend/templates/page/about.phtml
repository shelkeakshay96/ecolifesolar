<?php
/**
 * About page.
 *
 * @var \EcoLife\Cms\Block\Page $block
 */
$business = $block->getSetting('general/business/name', 'Eco Life');

$principles = [
    ['title' => 'We survey before we quote',
     'text'  => 'Shading changes through the day and through the year, and a roof that looks '
              . 'fine in a photograph often is not. We visit, we measure, and only then do we '
              . 'put a number on paper.'],
    ['title' => 'We will tell you not to buy',
     'text'  => 'If your consumption is low, or your roof is heavily shaded, or a water heater '
              . 'would save you more than panels would, we will say so. A system that '
              . 'disappoints is worse for us than a sale we did not make.'],
    ['title' => 'We stay reachable',
     'text'  => 'Installation is the beginning of a twenty-five year relationship with a piece '
              . 'of equipment on your roof. Being local is the whole point.'],
];
?>
<section class="relative overflow-hidden border-b border-gold-300/30 bg-pine-900">
    <div class="absolute inset-0 scale-110 bg-cover bg-center opacity-25"
         style="background-image:url('<?= $block->escapeUrl($block->getStaticUrl('images/panels2.jpg')) ?>')"
         data-parallax="0.08" aria-hidden="true"></div>
    <div class="absolute inset-0 bg-gradient-to-b from-pine-900/90 to-pine-900" aria-hidden="true"></div>

    <div class="section-shell relative z-10 max-w-3xl py-20 lg:py-24">
        <p class="eyebrow reveal text-gold-300"><span class="opacity-60">/02</span> Why Eco Life</p>
        <h1 class="reveal mt-3 text-4xl text-pearl sm:text-5xl" data-word-reveal>
            A renewable energy business, <em class="italic text-gold-300">in Satara.</em>
        </h1>
        <div class="gold-rule"></div>
        <?php /* The factual line, directly under the headline. It carries the founding
                 date and the parent group, without which the two company names in the
                 founder bios below read as an inconsistency. */ ?>
        <p class="reveal max-w-2xl font-mono text-[.78rem] uppercase tracking-[.1em] text-gold-300">
            Eco Life Green Infra LLP &mdash; the renewable-energy arm of Eco Life Group,
            founded 2002, based in Satara
        </p>

        <p class="reveal mt-5 max-w-2xl text-[1.02rem] leading-relaxed text-pine-100/90">
            <?= $block->escapeHtml($business) ?> designs, installs and maintains renewable
            energy systems across Satara and the surrounding district &mdash; rooftop solar,
            water heaters, solar pumps and street lighting. The people who survey your roof
            are the people who install the system, and the people who come back if something
            needs attention.
        </p>
    </div>
</section>

<section class="border-b border-sand bg-pearl-dim">
    <div class="section-shell py-16 lg:py-20">
        <h2 class="reveal text-3xl">How we work</h2>

        <div class="mt-10 grid gap-6 lg:grid-cols-3" data-reveal-stagger="110">
            <?php foreach ($principles as $principle): ?>
                <div class="reveal card card-lift border-t-2 border-t-gold-500">
                    <h3 class="text-[1.05rem]"><?= $block->escapeHtml($principle['title']) ?></h3>
                    <p class="mt-3 text-[.88rem] leading-relaxed text-muted">
                        <?= $block->escapeHtml($principle['text']) ?>
                    </p>
                </div>
            <?php endforeach; ?>
        </div>
    </div>
</section>

<?php /* Placed here rather than straight after the hero so both pages get the
         same light -> dark -> light rhythm. Straight after the hero would put
         pine-900 against pine-900 with only a hairline between them. */ ?>
<?= $block->renderChild(\EcoLife\Cms\Block\Stats::class, 'EcoLife_Cms::page/trust-stats.phtml') ?>

<?php /* ============================================================ team */ ?>
<section class="border-b border-sand bg-pearl">
    <div class="section-shell py-16 lg:py-20">
        <div class="reveal max-w-2xl">
            <p class="eyebrow">Who you will actually meet</p>
            <h2 class="mt-3 text-3xl">The people behind Eco Life Green Infra</h2>
            <p class="mt-4 text-[.95rem] leading-relaxed text-muted">
                Three people, all reachable, all in Satara. If something on your roof needs
                attention in year seven, one of them is who you will be speaking to.
            </p>
        </div>

        <div class="mt-10 grid gap-6 sm:grid-cols-2 lg:grid-cols-3" data-reveal-stagger="110">
            <?php foreach ($block->getTeam() as $person): ?>
                <article class="reveal card card-lift">
                    <?php if ($block->hasStaticFile($person['photo'])): ?>
                        <img src="<?= $block->escapeUrl($block->getStaticUrl($person['photo'])) ?>"
                             alt="<?= $block->escapeHtmlAttr($person['name']) ?>"
                             class="mb-5 aspect-[4/5] w-full rounded-xl border border-sand object-cover object-top"
                             loading="lazy" width="400" height="500">
                    <?php else: ?>
                        <?php /* Monogram until a photograph exists. A broken-image icon where a
                                 founder's face should be is worse than no photograph. */ ?>
                        <div class="mb-5 flex aspect-[4/5] w-full items-center justify-center rounded-xl
                                    border border-sand bg-pine-100"
                             aria-hidden="true">
                            <span class="font-display text-4xl font-semibold text-pine-700">
                                <?= $block->escapeHtml($block->getInitials($person['name'])) ?>
                            </span>
                        </div>
                    <?php endif; ?>

                    <h3 class="text-[1.05rem]"><?= $block->escapeHtml($person['name']) ?></h3>
                    <p class="mt-1 font-mono text-[.7rem] uppercase tracking-[.08em] text-gold-600">
                        <?= $block->escapeHtml($person['role']) ?>
                    </p>
                    <p class="mt-3 text-[.87rem] leading-relaxed text-muted">
                        <?= $block->escapeHtml($person['credential']) ?>
                    </p>
                </article>
            <?php endforeach; ?>
        </div>
    </div>
</section>

<section class="bg-pearl">
    <div class="section-shell max-w-3xl py-16 lg:py-20">
        <p class="eyebrow reveal"><span class="opacity-60">/03</span> How it works</p>
        <h2 class="reveal mt-3 text-3xl">What happens next</h2>

        <ol class="mt-10 space-y-7" data-reveal-stagger="100">
            <?php foreach ($block->getProcess() as $stage): ?>
                <li class="reveal flex gap-5">
                    <span class="mt-0.5 flex h-10 w-10 shrink-0 items-center justify-center rounded-full
                                 border border-gold-500 font-mono text-[.75rem] text-gold-600">
                        <?= $block->escapeHtml($stage['step']) ?>
                    </span>
                    <div>
                        <h3 class="text-[1.05rem]"><?= $block->escapeHtml($stage['title']) ?></h3>
                        <p class="mt-1.5 text-[.9rem] leading-relaxed text-muted"><?= $block->escapeHtml($stage['text']) ?></p>
                    </div>
                </li>
            <?php endforeach; ?>
        </ol>

        <div class="reveal mt-12">
            <a href="/contact" class="btn-gold btn-wide" data-magnetic="0.2">Book a free site visit</a>
        </div>
    </div>
</section>
