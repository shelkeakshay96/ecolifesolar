<?php
/**
 * Services page. Renders the same array the home page summarises, so the two
 * cannot drift apart.
 *
 * @var \EcoLife\Cms\Block\Page $block
 */
?>
<section class="border-b border-sand bg-pearl-dim">
    <div class="section-shell max-w-3xl py-16 lg:py-20">
        <p class="eyebrow reveal"><span class="opacity-60">/01</span> What we install</p>
        <h1 class="reveal mt-3 text-4xl sm:text-5xl">Rooftop solar, done right</h1>
        <p class="reveal mt-5 text-[1.02rem] leading-relaxed text-muted">
            Four services, all carried out by our own team. Everything is sized around your
            consumption and your roof rather than sold from a catalogue.
        </p>
    </div>
</section>

<section class="bg-pearl">
    <div class="section-shell max-w-5xl py-16 lg:py-20">
        <div class="space-y-7" data-reveal-stagger="110">
            <?php foreach ($block->getServices() as $index => $service): ?>
                <article class="reveal card card-lift">
                    <div class="flex items-start gap-5">
                        <span class="hidden shrink-0 text-pine-500 sm:inline-flex">
                            <?= $block->renderChild(\EcoLife\Core\View\Element\Template::class,
                                                    'EcoLife_Cms::page/service-icon.phtml',
                                                    ['icon' => $service['icon'], 'class' => 'h-16 w-16']) ?>
                        </span>

                        <div class="min-w-0 flex-1">
                            <div class="flex items-start justify-between gap-4">
                                <div class="flex flex-wrap items-center gap-3">
                                    <h2 class="text-xl sm:text-2xl"><?= $block->escapeHtml($service['title']) ?></h2>
                                    <?php if ($service['status'] === 'soon'): ?>
                                        <span class="rounded-full border border-sand-dark bg-pearl-dim px-2.5 py-0.5
                                                     font-mono text-[.62rem] uppercase tracking-[.1em] text-muted">
                                            Coming soon
                                        </span>
                                    <?php else: ?>
                                        <span class="rounded-full border border-pine-300/60 bg-pine-50 px-2.5 py-0.5
                                                     font-mono text-[.62rem] uppercase tracking-[.1em] text-pine-600">
                                            Available now
                                        </span>
                                    <?php endif; ?>
                                </div>
                                <span class="font-mono text-[.72rem] tracking-[.1em] text-sand-dark">
                                    <?= $block->escapeHtml(str_pad((string) ($index + 1), 2, '0', STR_PAD_LEFT)) ?>
                                </span>
                            </div>

                            <p class="mt-3 text-[.95rem] leading-relaxed text-muted">
                                <?= $block->escapeHtml($service['summary']) ?>
                            </p>

                            <ul class="mt-5 grid gap-2.5 sm:grid-cols-2">
                                <?php foreach ($service['points'] as $point): ?>
                                    <li class="flex gap-2.5 text-[.88rem]">
                                        <span class="mt-2 h-1.5 w-1.5 shrink-0 rounded-full bg-gold-500" aria-hidden="true"></span>
                                        <span><?= $block->escapeHtml($point) ?></span>
                                    </li>
                                <?php endforeach; ?>
                            </ul>
                        </div>
                    </div>
                </article>
            <?php endforeach; ?>
        </div>

        <?php /* Complete project execution: how all six above are delivered, rather
                 than a seventh service competing with them for attention. */ ?>
        <div class="reveal mt-10 card border-l-4 border-l-gold-500">
            <h2 class="text-[1.1rem]">Every one of these is a complete job</h2>
            <p class="mt-2.5 text-[.9rem] leading-relaxed text-muted">
                We do not hand you a box and a phone number. Whichever of the above you choose,
                the same four things are ours to deal with:
            </p>
            <ul class="mt-4 grid gap-2.5 sm:grid-cols-2">
                <?php foreach ($block->getExecutionSteps() as $step): ?>
                    <li class="flex gap-2.5 text-[.88rem]">
                        <span class="mt-2 h-1.5 w-1.5 shrink-0 rounded-full bg-gold-500" aria-hidden="true"></span>
                        <span><?= $block->escapeHtml($step) ?></span>
                    </li>
                <?php endforeach; ?>
            </ul>
        </div>

        <div class="reveal mt-12 flex flex-wrap gap-4">
            <a href="/contact" class="btn-gold btn-wide" data-magnetic="0.2">Request a site visit</a>
            <a href="/calculator" class="btn-secondary">Estimate your savings</a>
        </div>
    </div>
</section>
